import styled from 'styled-components'

export const UserRatingStyles = styled.div`
  display: flex;
  align-items: center;
  gap: 0.5rem;
  margin-top: 0.5rem;
  font-family: var(--font-wide);

  .stars {
    cursor: pointer;
    display: flex;
    align-items: center;
    gap: 0.2rem;
  }
`
